<div class="page-container page-margin c-page--404">
  <div class="empty-page-content text-center">
    <h1 class="heading">{{ 'general.404.title' | t }}</h1>
    <p class="para">{{ 'general.404.subtext' | t }}</p>
    <p>
      <a href="{{ routes.root_url }}" class="btn--primary btn--has-icon-after">{{ 'general.404.link' | t }}{% include 'icon-arrow-right' %}</a>
    </p>
  </div>
</div>
